before_install: if [[ `npm -v` != 3* ]]; then npm i -g npm@3; fi
install:
    - npm install
script:
    - npm run build
    - zip -r ratio-calculator.zip build/
addons:
    artifacts:
        paths:
            - ./ratio-calculator.zip
